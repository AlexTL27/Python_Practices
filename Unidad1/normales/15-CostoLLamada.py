print("\nCosto que tendrá una llamada telefónica con base en el tiempo que dura la llamada y el costo por minuto.")

min = float(input("Ingresa el precio por minutos:  "))

llamada = float(input("Ingresa la duración de la llamada:  "))

print("Total:  ", min * llamada)